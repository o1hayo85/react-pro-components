{
  "packages": ["packages/*"],
  "npmClient": "yarn",
  "version": "0.0.24",
  "command": {
    "publish": {
      "ignoreChanges": ["ignored-file", "*.md"],
      "message": "chore(release): publish",
      "registry": "https://registry.npmjs.org/"
    }
  }
}
